import 'package:flutter/material.dart';

class DisplayScreenHome extends StatelessWidget {
  const DisplayScreenHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Screen2')));
  }
}
