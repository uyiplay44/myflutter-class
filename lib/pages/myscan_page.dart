import 'package:flutter/material.dart';

class ScanScreenPage extends StatelessWidget {
  const ScanScreenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Screen3 ')));
  }
}
